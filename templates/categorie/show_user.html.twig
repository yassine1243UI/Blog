{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <h1>Categorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ categorie.titre }}</td>
            </tr>
                <ul>
        {% for article in articles %}
            <li>nom article : {{ article.titre }}</li>
        {% endfor %}
    </ul>
        </tbody>
    </table>

    <a href="{{ path('app_acceuil') }}">back to list</a>

    {% if is_granted('ROLE_ADMIN') %}

        {{ include('categorie/_delete_form.html.twig') }}

<a href="{{ path('app_categorie_edit', {'id': categorie.id}) }}">Modifier categoie</a>
    {% endif %}

{% endblock %}
